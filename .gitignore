# Never commit secrets
.ssh/
.secrets/
*.env
.wallet-password
scripts/status-hook/.post-token

# OS files
.DS_Store

# Claude Code local config (track skills, agents, settings.json; ignore the rest)
.claude/*
!.claude/skills/
!.claude/agents/
!.claude/settings.json

# Quest planning (local only)
.planning/

# Node / MCP
node_modules/
package.json
package-lock.json
.mcp.json

# Daemon runtime files
logs/
daemon/outbox/email-sent.jsonl
daemon/sales-proofs/*.firelog
daemon/.cycle-start-sha
